# Example environment variables
PORT=5000
MONGODB_URI=mongodb://localhost:27017/pet_adoption
# Set this to your deployed frontend origin (exact match). For local dev:
FRONTEND_ORIGIN=http://localhost:3000
